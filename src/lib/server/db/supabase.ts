// Future database entry point: create the Supabase server client here when persistence is introduced.
export const getSupabaseServerClient = () => {
  throw new Error(
    'Supabase is not configured for the mock dashboard milestone.'
  );
};
